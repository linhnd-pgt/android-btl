<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Android" parent="Base.Theme.Android" >
        <item name="colorPrimary">@color/base</item>

        <item name="colorAccent">@color/teal_200</item>
        <!-- thanh trạng thái màu trắng -->
        <item name="android:statusBarColor">@color/base</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="textAllCaps">false</item>
    </style>
    <style name="Widget.AppCompat.PopupMenu" parent="Widget.AppCompat.Light.PopupMenu">
        <item name="android:popupBackground">@android:color/white</item>
    </style>
</resources>